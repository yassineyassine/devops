#!/bin/bash

echo "Running commit-msg hook..."

# Lire le message de commit
commit_msg_file=$1
commit_msg=$(cat "$commit_msg_file")

# Regex pour valider le format
regex="^(feat|fix|docs|style|refactor|test|chore)[a-zA-Z0-9_-]+: .+$"

if [[ ! $commit_msg =~ $regex ]]; then
    echo "ERROR: Commit message format is invalid."
    echo "Expected format: <type>(<scope>): <message>"
    echo "Example: feat(UI): Add dark mode support"
    exit 1
fi

echo "Commit message is valid."
exit 0


git commit -m "Titre du commit^
^
Description détaillée avec un retour à la ligne."
<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN">
    <Appenders>
        <File name="alimsenapAppender" fileName="${catalina.base}/logs/alimsenap.log">
            <PatternLayout pattern="[%d] %-5p %c{2}:%L - %m%n"/>
        </File>
    </Appenders>

    <Loggers>
        <Logger name="fr.gouv.impots.appli.common.frameworks.gestionlogs.log4j.appenders.ArchiveDgfipFileAppender" level="INFO" additivity="false">
            <AppenderRef ref="alimsenapAppender"/>
        </Logger>

        <Logger name="fr.gouv.impots.appli.alimsenap" level="INFO" additivity="false">
            <AppenderRef ref="alimsenapAppender"/>
        </Logger>

        <Logger name="fr.gouv.impots.appli.common" level="ERROR" additivity="false">
            <AppenderRef ref="alimsenapAppender"/>
        </Logger>

        <Root level="INFO">
            <AppenderRef ref="alimsenapAppender"/>
        </Root>
    </Loggers>
</Configuration>
